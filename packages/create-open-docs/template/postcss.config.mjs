/**
 * Tailwind CSS v4 is configured entirely through the PostCSS plugin and the
 * `@theme` block in `styles/globals.css` — there is no `tailwind.config.js`.
 */
const config = {
  plugins: {
    '@tailwindcss/postcss': {},
  },
}

export default config
